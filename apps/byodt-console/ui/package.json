{
  "name": "@dethernety/byodt-console-ui",
  "version": "0.0.0",
  "private": true,
  "description": "The deployment console's operator SPA — status, failure-state reporting, and TLS. Self-contained (not a pnpm workspace member): install and build inside this directory with `pnpm install --ignore-workspace`. The build output is embedded into the Go console binary, so this package ships no runtime of its own.",
  "type": "module",
  "packageManager": "pnpm@9.13.2",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/roboto": "^5.3.0",
    "vue": "^3.5.41"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^25.9.5",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/test-utils": "^2.4.11",
    "happy-dom": "^20.11.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.9"
  }
}
