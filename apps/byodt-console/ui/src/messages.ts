// Operator-facing sentences that more than one panel shows.
//
// ONE SENTENCE, ONE DEFINITION, and a file rather than a constant beside one of its readers. Three panels
// show this line — the catalog, the artifacts and the disconnect control — and a copy per panel is the
// drift this rule exists to prevent: two literals whose whole purpose is being one literal. A file boundary
// is not a reason the rule stops applying.
//
// The daemon states the same fact in its own refusal, in Go, and that copy is deliberately NOT bound to
// this one: it is a REFUSAL — what happened, and that nothing changed — while this is a TOOLTIP explaining
// why a control is unavailable before anything is attempted. Different jobs, different sentences, and a
// parity test over prose would only make both harder to improve.

/** Why a deployment-changing control is unavailable, and who can change that. */
export const ADMIN_ONLY =
  'Only an administrator of this deployment\u2019s team can change what it provides. An owner or administrator of that team can grant you the administrator role in the portal.'
