# Dependencies
node_modules
**/node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Build Outputs
.next/
out/
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
.venv
apps/dt-ws/.env.bak
apps/dt-ws/.env copy

**/out*.txt
**/checkpoints*.db*

**/__pycache__/
**/.pytest_cache/
**/.mypy_cache/
**/.ipynb_checkpoints/
**/.vscode/

**/custom_modules/
**/custom_modules/*

venv

# Demo runtime data
demo/data/
demo/config/ssl/
demo/*.log
demo/.DS_Store
demo/login-client.pat

# Generated schema without @authentication (demo / development only)
apps/dt-ws/schema/schema-noauth.graphql

# Frontend build outputs served by dt-ws
apps/dt-ws/public/assets/
apps/dt-ws/public/index.html

# Console build-populated embed tree (noauth schema + data-module corpus copied in
# before `go build`; see apps/byodt-console/build-assets.sh). The sources are elsewhere
# in the tree; the copies are a build artifact.
apps/byodt-console/internal/assets/embed/

# Go binary build outputs
apps/byodt-console/byodt-console
apps/byodt-console/dist/

# Console operator SPA (self-contained --ignore-workspace package): its install tree and its
# Vite build output. The build output is copied into the embed tree above by build-assets.sh,
# so ui/dist is a transient artifact, not the embedded copy. The committed pnpm-lock.yaml is
# tracked — only these two are generated.
apps/byodt-console/ui/node_modules/
apps/byodt-console/ui/dist/
